server:
  port: 9898

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/NUTECH_TRX
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect